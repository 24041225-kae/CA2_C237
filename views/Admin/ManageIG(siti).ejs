<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Manage IGs | IGConnect Admin</title>

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" />

  <style>
    body {
      background-color: #f0f2f5;
      font-family: 'Segoe UI', sans-serif;
    }

    .page-title {
      background: linear-gradient(to right, #ffa726, #fb8c00);
      color: white;
      padding: 60px 0 40px;
      text-align: center;
      border-bottom-left-radius: 40px;
      border-bottom-right-radius: 40px;
      box-shadow: 0 8px 16px rgba(0,0,0,0.05);
    }

    .page-title h1 {
      font-weight: 700;
      font-size: 2.8rem;
      margin-bottom: 10px;
    }

    .page-title p {
      font-size: 1.1rem;
      opacity: 0.95;
    }

    .ig-card {
      background: white;
      border-radius: 16px;
      box-shadow: 0 6px 20px rgba(0,0,0,0.04);
      padding: 30px;
      margin-top: -50px;
      margin-bottom: 50px;
    }

    .btn-warning {
      background-color: #ffa726;
      border: none;
    }

    .btn-warning:hover {
      background-color: #fb8c00;
    }

    .ig-table th {
      background-color: #ffa726;
      color: white;
    }

    .btn-action {
      margin: 0 2px;
    }

    .table td, .table th {
      vertical-align: middle;
    }

    .alert {
      border-radius: 10px;
    }

    .not-found-img {
      filter: grayscale(0.5);
      opacity: 0.6;
    }

    @media (max-width: 768px) {
      .page-title h1 { font-size: 2rem; }
      .page-title p { font-size: 1rem; }
    }
  </style>
</head>
<body>

<<<<<<< HEAD
=======
<%- include('partials/Admin/admin-navbar') %>
>>>>>>> 0dd37e08ecd83a9669a31af625ab9ece5cf60b63
<!-- Header Section -->
<section class="page-title">
  <h1>Manage Interest Groups1</h1>
  <p>View, edit, and organize student Interest Groups.</p>
</section>

<!-- Main Card Container -->
<div class="container">
  <div class="ig-card">
    <div class="d-flex justify-content-between align-items-center mb-4">
      <h4 class="fw-bold mb-0">All Interest Groups</h4>
      <a href="/admin/interest-groups/add" class="btn btn-warning shadow-sm">
        <i class="fas fa-plus me-1"></i> Add New IG
      </a>
    </div>

    <% if (message && message.length > 0) { %>
      <div class="alert alert-warning"><%= message %></div>
    <% } %>
    <% if (successMsg && successMsg.length > 0) { %>
      <div class="alert alert-success"><%= successMsg %></div>
    <% } %>

    <% if (igList.length === 0) { %>
      <div class="text-center mt-5">
        <img src="/Images/NotFound.jpg" width="200" class="mb-3 not-found-img" alt="Not Found">
        <p class="text-muted">No Interest Groups found.</p>
      </div>
    <% } else { %>
      <div class="table-responsive">
        <table class="table table-hover ig-table align-middle">
          <thead>
            <tr>
              <th>#</th>
              <th>IG Name</th>
              <th>Category</th>
              <th>Advisor</th>
              <th>Members</th>
              <th class="text-center">Actions</th>
            </tr>
          </thead>
          <tbody>
            <% igList.forEach((ig, index) => { %>
              <tr>
                <td><%= index + 1 %></td>
                <td><%= ig.ig_name %></td>
                <td><%= ig.category %></td>
                <td><%= ig.advisor_name %></td>
                <td><%= ig.member_count %></td>
                <td class="text-center">
                  <a href="/admin/interest-groups/edit/<%= ig.id %>" class="btn btn-sm btn-primary btn-action shadow-sm">
                    <i class="fas fa-edit"></i>
                  </a>
                  <form action="/admin/interest-groups/delete/<%= ig.id %>" method="POST" class="d-inline">
                    <button type="submit" class="btn btn-sm btn-danger btn-action shadow-sm" onclick="return confirm('Are you sure you want to delete this IG?');">
                      <i class="fas fa-trash"></i>
                    </button>
                  </form>
                </td>
              </tr>
            <% }) %>
          </tbody>
        </table>
      </div>
    <% } %>
  </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
<%- include('partials/Admin/admin-footer') %>
</body>
</html>
