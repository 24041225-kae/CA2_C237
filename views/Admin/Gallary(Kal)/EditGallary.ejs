<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Edit Gallery | IGConnect Admin</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
  <style>
    body {
      background-color: #f8f9fa;
    }

    .header-section {
      position: relative;
      background: url('/img/gallery-header.jpg') no-repeat center center/cover;
      height: 240px;
      display: flex;
      align-items: center;
      justify-content: center;
      color: white;
    }

    .header-section::after {
      content: "";
      position: absolute;
      top: 0; left: 0;
      width: 100%; height: 100%;
      background: rgba(0, 0, 0, 0.5);
    }

    .header-title {
      position: relative;
      z-index: 1;
      font-size: 2.5rem;
      font-weight: bold;
    }

    .card {
      margin-top: -80px;
      z-index: 5;
      position: relative;
    }
  </style>
</head>
<body>

  <div class="header-section">
    <div class="header-title"><i class="fa-solid fa-pen-to-square"></i> Edit Gallery Submission</div>
  </div>

  <div class="container">
    <div class="row justify-content-center">
      <div class="col-lg-8">
        <div class="card shadow-sm p-4">
          <!-- Form for Editing Gallery, now with enctype="multipart/form-data" -->
      <form action="/admin/gallery/edit/<%= gallery.id %>" method="POST" enctype="multipart/form-data">
  <div class="mb-3">
    <label class="form-label"><i class="fa-solid fa-heading"></i> Title</label>
    <input type="text" class="form-control" name="title" value="<%= gallery.title %>" required>
  </div>

  <div class="mb-3">
    <label class="form-label"><i class="fa-solid fa-camera-retro"></i> Caption</label>
    <textarea class="form-control" name="caption" rows="2"><%= gallery.caption %></textarea>
  </div>



  <div class="mb-3">
    <label class="form-label"><i class="fa-solid fa-image"></i> Upload New Image</label>
    <input type="file" class="form-control" name="media_image">
  </div>

  <div class="mb-3">
    <label class="form-label"><i class="fa-solid fa-users"></i> IG ID</label>
    <input type="number" class="form-control" name="ig_id" value="<%= gallery.ig_id %>" required>
  </div>

  

  <div class="mb-3">
    <label class="form-label"><i class="fa-solid fa-calendar-day"></i> Upload Date</label>
    <input type="date" class="form-control" name="upload_date" value="<%= gallery.upload_date.toISOString().slice(0, 10) %>">
  </div>

  <div class="text-end">
    <button type="submit" class="btn btn-primary"><i class="fa-solid fa-floppy-disk"></i> Save Changes</button>
    <a href="/admin/gallery" class="btn btn-secondary"><i class="fa-solid fa-arrow-left"></i> Cancel</a>
  </div>
</form>


        </div>
      </div>
    </div>
  </div>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js" defer></script>

</body>
</html>
